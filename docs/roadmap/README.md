# 電商平台演進規劃

把現有的秒殺引擎擴充成完整電商平台的路線圖。

> **這份文件的前提**：作品集導向，目標是「架構決策的深度」而非「功能的廣度」。
> 每個項目都標了 `[展示]`（技術亮點）或 `[必要]`（少了就不成立）——
> 若目標轉為真實營運，把 `[必要]` 全部前移即可重排優先序。
>
> 工時估算假設：**一人開發、每週 15–20 小時**。

| 文件 | 內容 |
|------|------|
| 本文 | 核心架構主張、階段路線圖、風險 |
| [bounded-contexts.md](bounded-contexts.md) | 領域切分、模組演進、資料模型 |
| [frontend.md](frontend.md) | 前端架構、渲染策略、契約管理 |

---

## 一、現況盤點

### 已經有的

| 面向 | 現況 |
|------|------|
| 架構 | 六角架構模組化單體，4 模組，ArchUnit 強制分層 |
| 秒殺 | 四層削峰漏斗、Redis Lua 原子扣減、防超賣已實證 |
| 一致性 | Outbox + Saga 補償、三層冪等、庫存對帳 |
| 認證 | OAuth2 Resource Server + JWT |
| 可觀測性 | Micrometer + Prometheus + Grafana，9 條告警 |
| 測試 | 70 支（含 1000 執行緒防超賣） |
| 決策記錄 | 5 份 ADR |

### 還沒有的

資料庫只有 **3 張表**（`seckill_activity`、`seckill_order`、`outbox_event`）。
以電商平台的標準看，缺口是結構性的：

| 缺口 | 現況 | 嚴重度 |
|------|------|--------|
| **商品目錄** | 只有活動裡的 `productId` + 商品名快照，沒有商品實體 | 🔴 阻斷性 |
| **多品項訂單** | `SeckillOrder` 是單品項單活動，購物車結帳寫不進去 | 🔴 阻斷性 |
| **一般庫存** | 只有秒殺的 Redis 預熱庫存，一般商品無處扣減 | 🔴 阻斷性 |
| **付款** | 訂單停在 `PENDING_PAYMENT`，沒有任何東西能讓它變 `PAID` | 🔴 阻斷性 |
| **使用者實體** | 只有 JWT 的 `sub`，沒有會員資料、沒有收貨地址 | 🟠 高 |
| **Refresh token** | 令牌 2 小時過期就得重登，前端無法接受 | 🟠 高 |
| 購物車 | 無 | 🟠 高 |
| 履約物流 | 無 | 🟡 中 |
| ~~促銷優惠~~ ✅ | 已完成（[ADR-0013](../adr/0013-promotion-pricing-engine.md)）| — |
| 搜尋 | 無 | 🟡 中 |
| ~~評價~~ ✅ | 已完成（[ADR-0014](../adr/0014-review-and-rating-aggregate.md)）| — |
| ~~營運後台~~ ✅ | 已完成（[ADR-0015](../adr/0015-operations-console.md)）| — |
| 前端 | 無 | 🟠 高 |

---

## 二、核心架構主張

規劃的每個決定都從這五條推導出來。**先讀完這節，後面的階段安排才會有道理。**

### 主張 1：秒殺是特例通道，不是骨幹

這是整份規劃最重要的一條。

現有架構的**每一個設計**都建立在同一個前提上：
**流量極大、庫存極小、99.9% 的請求注定失敗**。

- 本機售罄標記 → 因為絕大多數請求會失敗
- 非同步下單（202 + 輪詢）→ 因為要把 DB 寫入移出熱路徑
- 庫存放 Redis → 因為所有請求競爭同一行資料
- 熱路徑禁 DB → 因為 DB 是吞吐瓶頸

**一般電商的流量特徵完全相反**：流量正常、SKU 數以萬計、絕大多數請求會成功、
領域規則複雜（優惠、稅、運費）、需要立即的交易一致性。

把一般下單也塞進秒殺通道會發生什麼：

| 秒殺的設計 | 套到一般下單的後果 |
|---|---|
| 非同步 202 + 輪詢 | 使用者按下「結帳」後要輪詢才知道買到沒——體驗災難 |
| 庫存預熱進 Redis | 五萬個 SKU 全部預熱？記憶體與預熱時間都不可行 |
| Lua 原子扣減 | 多品項 + 優惠券 + 運費計算，Lua 寫不下也不該寫 |
| 本機售罄標記 | 一般商品不會售罄，這層純粹是額外複雜度 |

**因此：兩條下單通道。**

```
                    ┌─────────────────────────────────┐
                    │  共用：使用者 · 商品 · 地址      │
                    │        付款 · 履約 · 通知        │
                    └───────────┬─────────────────────┘
                                │
              ┌─────────────────┴─────────────────┐
              │                                   │
    ┌─────────▼──────────┐            ┌──────────▼─────────┐
    │   一般下單通道      │            │   秒殺下單通道      │
    │                    │            │                    │
    │  同步、交易一致     │            │  非同步、最終一致   │
    │  DB 樂觀鎖扣庫存    │            │  Redis Lua 扣庫存   │
    │  直接回訂單        │            │  202 + 輪詢         │
    │                    │            │                    │
    │  最佳化目標：       │            │  最佳化目標：       │
    │  正確性與彈性       │            │  極致削峰           │
    └────────────────────┘            └────────────────────┘
```

兩條通道**共用領域模型與下游流程**，只在「庫存扣減機制」與「訂單建立路徑」分岔。

> 這條主張要寫成 **ADR-0006**。

### 主張 2：訂單聚合根的多品項重構必須早做

現在的 `SeckillOrder`：

```java
private final Long activityId;   // 單一活動
private final int quantity;      // 單一數量
private final BigDecimal amount; // 單一金額
```

這是**單品項**模型。購物車結帳（一次買 3 件不同商品）根本寫不進去。

必須重構成 `Order` + `OrderLine`：

```
Order (聚合根)
 ├── orderNo, userId, status, addressSnapshot, totalAmount
 └── List<OrderLine>
      └── skuId, skuSnapshot, unitPrice, quantity, subtotal
```

秒殺訂單則是「**只有一條 line 的 Order**」——特例，不是另一種型別。

**為什麼必須早做**：訂單是整個系統的樞紐，付款、履約、退款、對帳全都掛在它上面。
每晚一個階段重構，要一起改的下游就多一層。
這件事的成本會隨時間**指數上升**，拖到 P3 才做等於推倒重來。

> 這條主張要寫成 **ADR-0007**。

### 主張 3：庫存有兩種模型，由統一入口路由

| 商品類型 | 扣減機制 | 理由 |
|---|---|---|
| 一般商品 | MySQL 行 + 樂觀鎖 `UPDATE ... WHERE stock >= ?` | 衝突率低，DB 完全夠用，且天然有交易保證 |
| 秒殺商品 | Redis Lua（現有） | 所有請求競爭同一行，DB 鎖會塌陷 |

這正好呼應 [ADR-0002](../adr/0002-stock-in-redis-not-database.md) 已經寫下的
「重新評估的時機」：

> 若商品從「單一熱點」變成「數萬個低頻 SKU」，衝突不再集中，
> 資料庫樂觀鎖會是更簡單且足夠的方案。

**不是推翻舊決策，是舊決策預見到的分岔點到了。**

應用層只認得一個 `InventoryService`，依商品類型路由到對應實作——
呼叫端不需要知道背後是 Redis 還是 MySQL。

> 這條主張要寫成 **ADR-0008**。

### 主張 4：先切界限脈絡，模組拆分往後延

12 個界限脈絡（詳見 [bounded-contexts.md](bounded-contexts.md)），
但**不要一開始就切成 12 個 Maven 模組**——那會製造大量跨模組樣板，
而多數脈絡根本不需要獨立的生命週期。

演進策略：

```
現在        依「層」分模組，層內單一脈絡
  ↓
第一步      層內改為依「脈絡」分套件，ArchUnit 守跨脈絡邊界
  ↓
需要時      某脈絡證明需要獨立生命週期，才抽成獨立模組
  ↓
真的需要    才考慮拆成獨立服務（多半不會到這一步）
```

**判斷「該抽出去了」的訊號**：這個脈絡的變更頻率、擴縮需求、
或發布節奏明顯偏離其他脈絡。在那之前，套件邊界 + ArchUnit 已經足夠。

### 主張 5：前端是削峰漏斗的第 0 層

秒殺的四層漏斗目前從「本機售罄標記」開始。加上前端後應該是**五層**：

```
第 0 層  前端      按鈕禁用、本地節流、開賣時間校正、CDN 靜態頁
第 1 層  本機標記   Caffeine，奈秒級
第 2 層  多級快取   Caffeine → Redis
第 3 層  Redis Lua  唯一強一致點
第 4 層  Kafka      移出建單
```

**商品頁必須靜態化並由 CDN 完全承接**。100 萬次瀏覽若每次都打到 origin，
還沒到搶購按鈕就先被壓垮了。庫存數字則走獨立的輕量端點，與頁面本體解耦。

詳見 [frontend.md](frontend.md)。

---

## 三、階段路線圖

每個階段都是**垂直切片**（後端 + 前端），結束時都有能完整 demo 的東西。
不採「先做完所有後端再做前端」——那會讓 API 設計失去真實使用者的驗證。

---

### P0 — 讓現有秒殺真的能用（2–3 週）

現在的秒殺流程走到 `PENDING_PAYMENT` 就斷了。這個階段把它接通。

| 項目 | 標記 | 說明 |
|---|---|---|
| ~~Refresh token + 登出~~ ✅ | `[必要]` | 已完成：15 分鐘 access + 7 天 refresh，含輪替與重用偵測 |
| ~~使用者實體 + 註冊登入~~ ✅ | `[必要]` | 已完成：Identity 脈絡，`dev-token` 後門已移除 |
| ~~付款（沙盒或模擬）~~ ✅ | `[必要]` | 已完成：模擬閘道含非同步回調、簽章驗證、退款補償 |
| ~~秒殺前端頁~~ ✅ | `[必要]` | 已完成：ISR + 時鐘校正 + 退避輪詢 + BFF 令牌保管 |

**完成標準**：能從註冊 → 登入 → 搶購 → 付款 → 看到訂單，全程走完一次。
✅ **P0 已完成**，並以瀏覽器實測驗證整條流程。

**注意**：付款不要一開始就串真實金流。先做一個 `PaymentGateway` 埠 +
模擬實作，把訂單狀態機與 Saga 補償跑通；真實串接留到 P3。
先接金流會讓你困在對方的沙盒環境問題裡，而那與架構無關。

---

### P1 — 商品目錄與一般下單（4–6 週）⭐ 最關鍵的階段

| 項目 | 標記 | 說明 |
|---|---|---|
| ~~**Order 聚合根多品項重構**~~ ✅ | `[必要]` | 已完成：Order + OrderLine，秒殺是單行訂單 |
| ~~**Catalog 脈絡**~~ ✅ | `[必要]` | 已完成：SPU/SKU 分離、類目樹、活動改指向真實 SKU |
| ~~**Inventory 脈絡（雙模型）**~~ ✅ | `[必要]` | 已完成：MySQL 樂觀鎖 + Redis Lua，以劃撥隔開；對帳同步擴充三條恆等式 |
| ~~**一般下單流程（同步）**~~ ✅ | `[必要]` | 已完成：單一交易、201 + 完整訂單，關單直接退可售量 |
| ~~**前端：商品列表 + 詳情 + 直接購買**~~ ✅ | `[必要]` | 已完成：類目篩選、SKU 選擇、下單、付款 |

**完成標準**：能瀏覽商品、直接下單購買一般商品；秒殺流程不受影響仍正常。

✅ **P1 已完成**，並以瀏覽器實測整條流程（選規格 → 下單 → 付款）。

**風險最高的階段**。訂單重構會動到現有的 Outbox 事件、對帳邏輯、
以及 `RedisStockRepository` 的憑證格式。建議做法：

1. 先寫新的 `Order` + `OrderLine` 模型與測試
2. 讓 `SeckillOrder` 成為「單行 Order」的建構捷徑，不刪舊測試
3. 逐步把消費端、對帳、Outbox 遷移過去
4. 全綠後才刪除舊模型

**不要一次性替換**。這個聚合根有 70 支測試在保護它，善用它們。

---

### P2 — 購物車與結帳（3–4 週）

| 項目 | 標記 | 說明 |
|---|---|---|
| ~~**Cart 脈絡**~~ ✅ | `[必要]` | 已完成：未登入用 localStorage，登入後取較大值合併 |
| ~~**收貨地址**~~ ✅ | `[必要]` | 已完成：地址簿 CRUD、下單時快照進訂單，改地址不影響已成立訂單 |
| ~~**結帳流程**~~ ✅ | `[必要]` | 已完成：與直接下單共用建立路徑，成功後同交易清空購物車 |
| ~~**前端：購物車 + 結帳頁**~~ ✅ | `[必要]` | 已完成：本地購物車、登入合併、結帳頁 |

**完成標準**：完整的購物體驗——加購物車、改數量、選地址、結帳。

✅ **P2 已完成**，並以瀏覽器實測整條流程（未登入加購物車 → 登入自動合併 → 結帳）。

**一個容易錯的細節**：訂單裡的地址、商品名、價格都必須是**快照**。
使用者改了地址或商家調了價，歷史訂單不能跟著變——那是財務問題，不是顯示問題。

---

### P3 — 履約與售後（4–5 週）

| 項目 | 標記 | 說明 |
|---|---|---|
| 付款真實串接 ⏸ | `[必要]` | **需要沙盒金鑰**才能實機驗證，暫緩 |
| ~~**Fulfillment 脈絡**~~ ✅ | `[必要]` | 已完成：出貨單狀態機、配送失敗可重送、訂單里程碑同步 |
| ~~**退款退貨**~~ ✅ | `[必要]` | 已完成：ReturnRequest 聚合根、部分退款、三層防重複、退款 Saga（[ADR-0011](../adr/0011-refund-saga.md)）|
| ~~**Notification 脈絡**~~ ✅ | `[展示]` | 已完成：站內信與 Email 雙管道、內容快照、過期事件不通知 |

**完成標準**：訂單全生命週期閉環——下單、付款、出貨、收貨、退款。**P3 已完成。**

Notification 做下來最有價值的一課是**實機第一次啟動才發現的**：
消費組是新的、`auto-offset-reset` 是 `earliest`，於是它把 topic 上整段歷史
重播了一遍——1.6 秒內產生 80 筆通知，內容是幾個月前就已經送達的訂單。

那不是設定錯誤，是「新消費組加入既有 topic」的正常行為，
而且會在每一次「新增通知管道」或「重設消費位移」時重現。
因此防線放在應用層（事件超過一天就不通知）而不是 Kafka 設定：
設定會被改、環境之間也不一致，而「三個月前的出貨通知是雜訊不是資訊」
這個判斷在任何設定下都成立。

**退款確實是第二個 Saga**，而做下去才看清楚難的不是「兩件事都要還原」，
是**兩件事在不同時間點還原**——錢可以馬上退，貨要等買家寄回，
中間隔著好幾天。因此流程被拆成「核准」與「驗收」兩段。

實作時冒出三件事前沒想到的：

1. **已退款的訂單在秒殺對帳裡仍必須算作佔用庫存**。退回的貨進的是一般庫存，
   Redis 餘量不會增加；不算它，對帳會報 STOCK_LEAKED 卻什麼都沒洩漏
2. **庫存流水的來源要記退貨單號而非訂單號**。唯一鍵含來源單號，
   而一張訂單可以有多張退貨單——都記訂單號，第二張的回補會被安靜略過
3. **退款的冪等最終只能由金流閘道保證**。「請求已送達但回應遺失」
   這個狀態只有對方知道，我們能做的是每次重試都送同一把冪等鍵

---

### 已知缺口

以下是抓 bug 時發現、但修法超出當下範圍的：

| 缺口 | 後果 | 修法 |
|---|---|---|
| ~~**活動快取沒有失效路徑**~~ ✅ | 已補上上下架端點；寫入後主動清 L2 與本機 L1，其他節點最慢 5 秒（L1 TTL）看到新狀態。直接改資料庫仍繞得過去——那條路沒有地方可以掛失效邏輯 |
| ~~**搜尋索引與資料庫沒有對帳**~~ ✅ | 已完成：每 15 分鐘比對兩邊的 ID 集合，兩個方向分開回報，並預設自動修復（修復動作與待消費事件會做的事一模一樣，因此可證明安全）|
| **舊索引沒有保留策略** | 每重建一次就永久多一份完整副本，磁碟只增不減 | 保留最近 N 個版本，其餘刪除 |
| ~~**Outbox 中繼器逐筆同步等 ack**~~ ✅ | 已改成兩階段投遞，實測 45 → 約 200 事件/秒。瓶頸現在是 `batch-size` × 排程間隔（可調），不再是投遞本身 |

---

### P4 — 營運與成長（持續）

| 項目 | 標記 | 說明 |
|---|---|---|
| ~~**搜尋（ES + CQRS 讀模型）**~~ ✅ | `[展示]` ⭐ | 已完成：Outbox 驅動索引、alias 零停機重建、ES 故障時降級回資料庫（[ADR-0012](../adr/0012-search-read-model.md)）|
| ~~**營運後台**~~ ✅ | `[必要]` | 已完成：出貨、退貨審核、商品上下架與建立、活動配置、維運工具（[ADR-0015](../adr/0015-operations-console.md)）|
| ~~**Promotion 脈絡**~~ ✅ | `[展示]` ⭐ | 已完成：純函式計價引擎、折扣快照進訂單、券以條件式 UPDATE 核銷（[ADR-0013](../adr/0013-promotion-pricing-engine.md)）|
| ~~**Review 脈絡**~~ ✅ | `[必要]` | 已完成：只有買過的人能評、評分聚合可增量維護（[ADR-0014](../adr/0014-review-and-rating-aggregate.md)）|

**搜尋是這個階段的重點**。用 Outbox 事件同步到 Elasticsearch，
是「事件驅動 + CQRS 讀模型」的教科書級應用，而且**你已經有 Outbox 了**——
邊際成本低、展示價值高。

價格計算引擎（多重優惠疊加、互斥規則）也是很好的領域建模題目，
**做的時候順手挖出一個真的會流錢的缺陷**：退款原本用 `unitPrice × quantity` 算，
而整單折扣是折在訂單上、退貨卻是退一行——有折扣的訂單，那個乘積是使用者
沒有付過的錢。全額退貨會被付款金額上限擋下，但**部分退貨不會**，
那筆多退的錢仍在上限之內。修法是把折後金額分攤回每一行存起來（`order_line.allocated_amount`），
退款按累計分攤算。

---

### P5 — 規模化（按需，多半不需要）

| 項目 | 說明 |
|---|---|
| 讀寫分離 | 主從複製 + 讀路由 |
| 分庫分表 | 訂單按 userId 分片 |
| 服務拆分 | 僅當某脈絡真的證明需要獨立生命週期 |

**這個階段預設不做。** 沒有真實流量壓力就做分庫分表，
只會得到一個複雜且無法驗證的系統——那是反效果，不是加分。

若要展示這方面能力，**寫一份「規模化路徑」的 ADR** 說明
「什麼指標達到什麼閾值時該做什麼」，比實際做了更能展現判斷力。

---

## 四、需要新增的 ADR

| 編號 | 主題 | 階段 |
|------|------|------|
| [ADR-0006](../adr/0006-dual-order-channels.md) ✅ | 雙下單通道：秒殺與一般下單分離 | P1 前 |
| [ADR-0007](../adr/0007-multi-line-order-aggregate.md) ✅ | 訂單聚合根多品項重構 | P1 前 |
| [ADR-0008](../adr/0008-dual-inventory-model.md) ✅ | 庫存雙模型與統一路由 | P1 前 |
| ADR-0009 | 前端框架與渲染策略 | P0 |
| ADR-0010 | 界限脈絡切分與模組演進策略 | P1 前 |
| [ADR-0012](../adr/0012-search-read-model.md) ✅ | 搜尋讀模型與最終一致窗口 | P4 |
| [ADR-0013](../adr/0013-promotion-pricing-engine.md) ✅ | 優惠計算引擎與折扣的快照邊界 | P4 |
| [ADR-0014](../adr/0014-review-and-rating-aggregate.md) ✅ | 評價的可信度與評分聚合 | P4 |
| [ADR-0015](../adr/0015-operations-console.md) ✅ | 營運後台的邊界 | P4 |

**ADR 先於程式碼。** P1 的三份（0006–0008）**已完成** —— 它們互相牽連，
因此一起寫：通道決定庫存機制，庫存機制決定訂單建立路徑。

---

## 五、風險與反模式

### 🔴 會毀掉架構的

**把一般下單塞進秒殺通道。** 主張 1 的反面。症狀是開始出現
「為什麼買一本書也要輪詢」、「這個 SKU 忘了預熱所以買不了」。
一旦走上這條路，兩種場景的最佳化目標會互相拉扯，最後兩邊都做不好。

**訂單重構拖到 P2 之後。** 成本隨掛在訂單上的下游數量指數上升。
P1 做要改 3 個地方，P3 做要改 15 個。

**過早拆微服務。** 現在拆＝把方法呼叫換成網路呼叫，
換來分散式交易、服務發現、鏈路追蹤的全套成本，而收益是零。
[ADR-0001](../adr/0001-modular-monolith-hexagonal.md) 已經有結論。

**God Order 聚合根。** 把秒殺、一般、預購、團購全部塞進同一個聚合根，
用一堆 `if (type == ...)` 分支。正確做法是共用 `Order` 的**結構**，
差異放在建立路徑與策略物件。

### 🟠 會慢慢腐蝕的

**前端直接打領域 API。** 商品頁需要商品 + 庫存 + 評價 + 促銷，
若前端自己發 4 個請求聚合，後端任何重構都會打破前端。
應該有 BFF 層或聚合端點。

**前後端契約漂移。** 建議從 OpenAPI 自動產生 TypeScript 型別
（`openapi-typescript`），讓後端改欄位時前端**編譯就失敗**，
而不是上線後才在瀏覽器 console 發現。

**Outbox 表隨訂單量膨脹。** 已有清理排程，但隨脈絡增加、事件種類變多，
要重新評估保留天數與索引。

**所有商品都預熱進 Redis。** 主張 3 的反面。只有秒殺商品該預熱。

**對帳偵測得到、卻不自動修的洩漏。** 目前自動修復只涵蓋「孤兒扣減」
（訂單不存在），另一種情況沒被涵蓋：**訂單已 CANCELLED 但 Redis 憑證還在**。
實測時撞到過一次（drift = −1），來源是繞過聚合根直接改狀態，
所以沒有發出 `OrderCancelledEvent`，退庫自然不會發生。

正式營運不會有人直接改資料庫，但 **Outbox 投遞失敗、消費端反覆進 DLQ**
會造出一模一樣的狀態。這種洩漏其實可以安全自動修——
退庫走的是同一張憑證映射（`seckill_restore.lua` 冪等），
就算補償訊息稍後才被消費，第二次也只會 no-op。

Inventory 雙模型那一步已補上三條對帳恆等式（含「Redis 有庫存卻無劃撥支撐」），
但**這一種洩漏仍未納入自動修復**——它需要判斷補償訊息是否還在佇列裡，
而對帳看不到佇列。`OVERSELL_RISK` 方向也維持不自動修。

### 🟡 要留意的

- **快照 vs 引用**：訂單裡的價格、商品名、地址必須快照
- **時區**：現在全部用 UTC `Instant`，前端顯示時才轉——別在後端做時區轉換
- **金額精度**：一律 `BigDecimal`，永遠不要用 `double`
- **測試會變慢**：脈絡變多後 Testcontainers 的啟動成本會累積，
  及早規劃共用容器（`@Testcontainers` 的 singleton 模式）

---

## 六、建議的下一步

不要一次規劃到底。實際的下一步只有三件：

1. **寫 ADR-0006 / 0007 / 0008**（雙通道、訂單重構、庫存雙模型）
   —— 這三個互相牽連，一起想清楚
2. **做 P0 的 refresh token + 使用者實體** —— 範圍小、風險低、
   而且是所有前端工作的前提
3. **搭前端骨架**，先只做秒殺頁 —— 用真實前端驗證現有 API 的可用性，
   會很快暴露出目前 API 設計的問題

P0 完成後再回頭修正這份規劃。**路線圖是用來被修改的，不是用來遵守的。**
